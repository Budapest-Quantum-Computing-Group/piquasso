.tox
dist
build

__pycache__
.pytest_cache
.mypy_cache
piquasso.egg-info/
venv/
.venv/
coverage.xml
.coverage
prof

# IDEs
.vscode
.idea

# Sphinx
docs/_build
.ipynb_checkpoints/

# Tensorboard
logdir/

# Shared libraries
*.so
*.pyd

# CMake build artifacts that leak into wheels when building locally
# after a manual cmake run (cibuildwheel is unaffected — it uses a clean checkout)
CMakeFiles/
CMakeCache.txt
CMakeInit.txt
cmake_install.cmake
build.ninja
.ninja_deps
.ninja_log
.skbuild-info.json
*.o
